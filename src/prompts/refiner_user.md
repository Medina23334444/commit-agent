Mensaje rechazado: {message}

Razón del rechazo: {critica}

Intención real del cambio: {intencion}
Tipo detectado: {tipo_detectado}
Scope detectado: {scope_detectado}

RESTRICCIÓN CRÍTICA DE LONGITUD:
La primera línea tiene EXACTAMENTE 72 caracteres como máximo.
Cuenta los caracteres antes de responder.
Si tu mensaje tiene más de 72 chars, acórtalo eliminando palabras.

Ejemplo de acortar:
- `test(conversion): add integration tests for crypto_utils and nginx config` (73 chars) ❌
- `test(conversion): add crypto_utils and nginx integration tests` (62 chars) ✅

Diff resumido:
{diff_resumido}

Corrige el mensaje respetando la intención real del cambio.